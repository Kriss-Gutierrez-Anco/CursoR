#Nombre: Kriss Gutierrez Anco
#Respuesta a la pregunta 2
#Una funcion vectorizada  es una funcion que puede aceptar como parametros de entrada a un vector, realizando funciones con este vector por ejemplo :
a<-c(1,9,5,8)
a+1 #Suma a cada componente del vector una unidad
#Al realizar operaciones con vectores ya que las operaciones se hacen componente a componente si alguno de los vectores no tuviera la longitud de los otros R hace un autocompletado del vector añadiendo los componentes faltantes concatenando el mismo vector hasta que quede completo
a<-c(1,8,6,4,5,2)
b<-c(9,2)
a+b #el vector b se autocompleta repitendo sus componentes
  
